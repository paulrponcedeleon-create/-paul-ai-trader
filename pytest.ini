[pytest]
markers =
    unit: pure tests that do not require FastAPI, SQLAlchemy, Alembic, network, or Bitso
    integration: local integration tests
    api: tests requiring FastAPI/Pydantic/TestClient
    database: tests requiring SQLAlchemy or Alembic
