name: v2-ci

on:
  pull_request:
    branches:
      - v2-dashboard
  push:
    branches:
      - v2-dashboard

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PYTHONPATH: .

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Run tests
        id: pytest
        shell: bash
        run: |
          set +e
          python -m pytest -q --tb=short > pytest-output.txt 2>&1
          exit_code=$?
          cat pytest-output.txt
          echo "exit_code=$exit_code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Publish pytest failure on PR
        if: steps.pytest.outputs.exit_code != '0' && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('pytest-output.txt', 'utf8');
            const excerpt = output.slice(-6000);
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: `### v2-ci: pytest falló\n\n\`\`\`text\n${excerpt}\n\`\`\``
            });

      - name: Stop after pytest failure
        if: steps.pytest.outputs.exit_code != '0'
        run: exit 1

      - name: Validate Alembic migration
        env:
          DATABASE_URL: sqlite:////tmp/paul_ai_trader_ci.db
        run: |
          rm -f /tmp/paul_ai_trader_ci.db
          python -m alembic upgrade head
          python - <<'PY'
          import sqlite3

          connection = sqlite3.connect("/tmp/paul_ai_trader_ci.db")
          tables = {
              row[0]
              for row in connection.execute(
                  "SELECT name FROM sqlite_master WHERE type='table'"
              )
          }
          indexes = {
              row[0]
              for row in connection.execute(
                  "SELECT name FROM sqlite_master WHERE type='index'"
              )
          }
          columns = {
              row[1]
              for row in connection.execute("PRAGMA table_info(simulated_orders)")
          }
          order_event_columns = {
              row[1]
              for row in connection.execute(
                  "PRAGMA table_info(simulated_order_events)"
              )
          }
          revision = connection.execute(
              "SELECT version_num FROM alembic_version"
          ).fetchone()
          connection.close()

          assert "alembic_version" in tables
          assert "simulated_orders" in tables
          assert "simulated_order_events" in tables
          assert "backtest_runs" in tables
          assert "optimization_runs" in tables
          assert "paper_accounts" in tables
          assert "analytics_snapshots" in tables
          assert "analytics_events" in tables
          assert "ix_simulated_orders_book" in indexes
          assert "ix_simulated_orders_correlation_id" in indexes
          assert "ix_simulated_orders_status" in indexes
          assert "ix_simulated_order_events_created_at" in indexes
          assert "ix_simulated_order_events_position_id" in indexes
          assert "ix_simulated_order_events_side" in indexes
          assert "ix_simulated_order_events_source" in indexes
          assert "ix_analytics_snapshots_created_at" in indexes
          assert "ix_analytics_events_timestamp" in indexes
          assert {
              "closed_at",
              "close_price",
              "realized_pnl_mxn",
              "entry_fee_rate",
              "entry_fee_mxn",
              "exit_fee_rate",
              "exit_fee_mxn",
          }.issubset(columns)
          assert {
              "position_id",
              "book",
              "side",
              "status",
              "amount_mxn",
              "price",
              "fee_mxn",
              "realized_pnl_mxn",
              "source",
              "reason",
              "correlation_id",
          }.issubset(order_event_columns)
          assert revision == ("20260723_0008",)
          PY
